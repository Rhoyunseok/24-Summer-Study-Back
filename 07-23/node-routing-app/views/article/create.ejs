<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        textarea {
            width: 500px;
            height: 300px;
        }
        
    </style>
</head>
<body>
    <h1><%= title %></h1>

<!--article/create 주소로 post방식으로 form 태그 내 사용자 입력/선택 폼데이터를 전송합니다-->
<!--submit 은 클라이언트에서 서버로 데이터를 전송한다는 의미고 submit 버튼일 클릭되면 form 태그의 onSubmit이벤트가 발생합니다.-->
    <form method="post" action="/article/create" onSubmit="return checkForm();">
        제목: <input type="text" id="title" name="title"/><br/>
        내용: <br/>
        <textarea id="contents" name="contents"></textarea><br/>
        게시여부: 
        <select id="display" name="display">
            <option value="9">선택</option>
            <option value = "1";>게시함</option>
            <option value = "0">게시안함</option>
        </select>
        <br/>
        <input type="submit" value="저장"/>
    </form>

    <script>
        //클라이언트 폼 유효성 검사 함수 기능 정의
        function checkForm() {
            if(document.getElementById("title").value == "") {
                alert("게시글 제목을 입력하세요.");
                document.getElementById("title").focus();
                //진행상황을 멈춰라
                return false;
            }
            //서버로 폼데이터를 전송해라
            return true;
        }
    </script>
</body>
</html>